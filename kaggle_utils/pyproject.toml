[project]
name = "kaggle_utils"
version = "0.1.0"
description = "Common utilities for Kaggle competitions"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "numpy",
    "torch",
    "scikit-learn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
